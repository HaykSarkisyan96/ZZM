# Как найти сервис с bot_api.py в Railway

## Способ 1: По логам (самый простой)

1. Зайдите в Railway Dashboard: https://railway.app
2. Откройте ваш проект
3. Посмотрите на все сервисы в проекте
4. Для каждого сервиса:
   - Нажмите на сервис
   - Перейдите в раздел **"Deployments"** или **"Logs"**
   - Посмотрите логи запуска
   
   **Ищите в логах:**
   - `Running on http://0.0.0.0:PORT` или `Running on http://127.0.0.1:PORT`
   - `Serving Flask app 'bot_api'`
   - `* Running on all addresses`
   - Или просто текст из `bot_api.py` при запуске

## Способ 2: По Procfile

1. В Railway Dashboard откройте каждый сервис
2. Перейдите в **Settings** → **Build & Deploy**
3. Посмотрите на **Start Command** или проверьте файл Procfile в коде
4. Сервис с `bot_api.py` будет иметь:
   - `web: python bot_api.py` в Procfile
   - Или Start Command: `python bot_api.py`

## Способ 3: По переменным окружения

1. Откройте каждый сервис в Railway
2. Перейдите в **Variables** (переменные окружения)
3. Сервис с `bot_api.py` обычно имеет:
   - `PORT` (Railway установит автоматически)
   - `BOT_TOKEN`
   - Возможно `BOT_URL` или `API_URL`

## Способ 4: По публичному домену (если есть)

1. Откройте каждый сервис
2. Перейдите в **Settings** → **Networking**
3. Если у сервиса есть **Public Domain** - это может быть API сервис
4. Проверьте URL: `https://your-domain.up.railway.app/health`
5. Если возвращает `{"status":"ok"}` - это API сервис!

## Способ 5: По названию сервиса

Обычно сервисы называются так:
- `api` или `bot-api` - для bot_api.py
- `bot` или `telegram-bot` - для bot.py
- `web` или `landing` - для лендинга

Но это не всегда так, лучше проверить по логам.

## ✅ Быстрая проверка

**Самый надежный способ:**

1. Откройте Railway Dashboard
2. Для каждого сервиса:
   - Нажмите на сервис
   - Откройте **Logs** (вкладка вверху)
   - Посмотрите последние логи
   - Если видите `Serving Flask app 'bot_api'` или `Running on http://0.0.0.0` - это API сервис!

## 🔍 Пример того, что искать в логах:

```
INFO:werkzeug: * Running on all addresses (0.0.0.0)
INFO:werkzeug: * Running on http://127.0.0.1:3001
INFO:werkzeug: * Running on http://10.0.0.2:3001
```

Или:

```
Serving Flask app 'bot_api'
 * Debug mode: on
```

## 📝 После того как нашли:

1. Запишите название сервиса
2. Перейдите в **Settings** → **Networking**
3. Скопируйте **Public Domain** (если есть)
4. Используйте этот URL в `calculate.js`:
   ```javascript
   const API_URL = 'https://your-domain.up.railway.app/api/process';
   ```

