# Диагностика проблемы с автоматической отправкой

## 🔍 Шаг 1: Проверка переменной BOT_URL

### В Railway для API сервиса:

1. Откройте сервис **API** (где `bot_api.py`)
2. Перейдите в **Variables**
3. Найдите переменную `BOT_URL`
4. **Скопируйте значение** и сообщите мне

**Что проверить:**
- ✅ Переменная называется именно `BOT_URL` (без пробелов)
- ✅ Значение начинается с `https://`
- ✅ Значение НЕ содержит `/api/...` в конце
- ✅ Значение НЕ содержит пробелов

## 🔍 Шаг 2: Проверка HTTP endpoint бота

### Тест 1: Проверка доступности бота

Откройте в браузере (замените на ваш URL):
```
https://ваш-tgbot-домен.up.railway.app/health
```

**Ожидаемый результат:**
```json
{"status": "ok", "service": "telegram_bot"}
```

**Если не работает:**
- Бот не имеет публичного домена
- HTTP сервер бота не запущен
- Проблема с настройками Railway

### Тест 2: Проверка endpoint для chat_id

Откройте в браузере (замените `test_username` на любой username):
```
https://ваш-tgbot-домен.up.railway.app/api/user_chat_id/test_username
```

**Ожидаемый результат (если пользователь не писал боту):**
```json
{
  "success": false,
  "message": "Chat ID not found",
  "username": "test_username"
}
```

**Если видите ошибку 404 или другую:**
- Endpoint не работает
- Проблема с маршрутизацией

## 🔍 Шаг 3: Проверка логов API

### В Railway для API сервиса:

1. Откройте сервис **API**
2. Перейдите в **Logs** (вкладка вверху)
3. Загрузите файл на сайте
4. **Скопируйте последние логи** и отправьте мне

**Что искать в логах:**

✅ **Хорошие логи (работает):**
```
Запрос chat_id для username через https://...
Chat ID для пользователя username получен из TGBOT: 123456789
✅ Заявка автоматически отправлена пользователю username
```

❌ **Плохие логи (не работает):**
```
BOT_URL не указан, не могу получить chat_id из TGBOT
```
→ Переменная `BOT_URL` не установлена

```
Ошибка при запросе chat_id из TGBOT: ...
```
→ Проблема с подключением к боту

```
Chat ID для пользователя username не найден в TGBOT (пользователь еще не писал боту)
```
→ Это нормально для первого использования

## 🔍 Шаг 4: Проверка на сайте

### Тест:

1. Загрузите файл на сайте (пользователь, который УЖЕ писал `/start` в боте ранее)
2. Посмотрите, какое сообщение появилось:
   - ✅ "✅ Заявка успешно отправлена в Telegram!" → Работает!
   - ❌ "Заявка готова! Напишите боту @... команду /start" → Не работает

## 🔍 Шаг 5: Проверка настроек бота

### В Railway для TGBOT сервиса:

1. Откройте сервис **TGBOT** (где `bot.py`)
2. Перейдите в **Settings** → **Networking**
3. Проверьте, есть ли **Public Domain**
4. Если нет → нажмите **"Generate Domain"**

**Важно:** Бот должен иметь публичный домен для работы HTTP endpoint!

## 📋 Чеклист для отправки мне

Пришлите мне следующую информацию:

1. **Значение `BOT_URL`** из Variables API сервиса
2. **Public Domain** сервиса TGBOT
3. **Результат теста** `/health` endpoint бота
4. **Результат теста** `/api/user_chat_id/test_username` endpoint бота
5. **Логи API** после загрузки файла
6. **Какое сообщение** появляется на сайте при загрузке файла


