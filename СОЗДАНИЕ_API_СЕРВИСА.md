# Создание нового API сервиса в Railway

## 🗑️ Шаг 1: Удаление старого Flask сервиса (опционально)

Если старый Flask сервис больше не нужен:

1. В Railway Dashboard откройте сервис "Сайт, он же Flask"
2. Перейдите в **Settings** → **Danger Zone**
3. Нажмите **"Delete Service"**
4. Подтвердите удаление

**⚠️ Внимание**: Убедитесь, что этот сервис действительно не нужен!

## 🆕 Шаг 2: Создание нового API сервиса

### Вариант A: Через Railway Dashboard (Рекомендуется)

1. В Railway Dashboard откройте ваш проект
2. Нажмите **"New"** → **"Empty Service"** или **"GitHub Repo"**
3. Если **GitHub Repo**:
   - Выберите ваш репозиторий с `bot_api.py`
   - Railway создаст новый сервис
4. Если **Empty Service**:
   - Нажмите **"Deploy from GitHub repo"**
   - Выберите ваш репозиторий
   - Railway создаст сервис

### Вариант B: Через Railway CLI

```bash
# Установите Railway CLI если еще не установлен
npm i -g @railway/cli

# Войдите в Railway
railway login

# Создайте новый сервис
railway init
railway link
railway up
```

## ⚙️ Шаг 3: Настройка нового API сервиса

### 3.1. Настройка запуска

1. Откройте созданный сервис
2. Перейдите в **Settings** → **Build & Deploy**
3. В разделе **Start Command** укажите:
   ```
   python bot_api.py
   ```
   
   Или создайте файл `Procfile` в корне репозитория:
   ```
   web: python bot_api.py
   ```

### 3.2. Переменные окружения

Перейдите в **Variables** и добавьте:

```
BOT_TOKEN=ваш_токен_бота
PORT=3001
BOT_URL=https://ваш-tgbot-сервис.up.railway.app (если используется)
API_URL=https://ваш-новый-api-сервис.up.railway.app (после получения домена)
BOT_USERNAME=ваш_бот_username (опционально)
```

**Где взять BOT_TOKEN?**
- Тот же токен, что используется в сервисе TGBOT

### 3.3. Получение Public Domain

1. Перейдите в **Settings** → **Networking**
2. Нажмите **"Generate Domain"** или **"Custom Domain"**
3. Скопируйте полученный URL (например: `https://api-production-abc123.up.railway.app`)

## ✅ Шаг 4: Проверка работы API

1. Дождитесь завершения деплоя (статус "Running")
2. Откройте в браузере: `https://ваш-api-домен.up.railway.app/health`
3. Должно вернуть: `{"status":"ok"}`

Если вернуло - API работает! 🎉

## 🔗 Шаг 5: Обновление ссылок

### 5.1. Обновить calculate.js

Откройте файл `/Users/hayksarkisyan/Desktop/New Landing/calculate.js` и измените:

```javascript
const API_URL = 'https://ваш-новый-api-домен.up.railway.app/api/process';
```

### 5.2. Обновить переменные окружения в TGBOT сервисе

1. Откройте сервис **TGBOT** в Railway
2. Перейдите в **Variables**
3. Убедитесь, что установлена переменная:
   ```
   API_URL=https://ваш-новый-api-домен.up.railway.app
   ```

## 📋 Чеклист

- [ ] Старый Flask сервис удален (если не нужен)
- [ ] Новый API сервис создан
- [ ] Start Command установлен: `python bot_api.py`
- [ ] Переменные окружения установлены (BOT_TOKEN, PORT)
- [ ] Public Domain получен
- [ ] `/health` endpoint возвращает `{"status":"ok"}`
- [ ] `calculate.js` обновлен с новым API URL
- [ ] TGBOT сервис обновлен с новым API_URL

## 🎯 Итоговая структура сервисов

После настройки у вас будет:

1. **API сервис** (`bot_api.py`)
   - Обрабатывает запросы с лендинга
   - Endpoint: `/api/process`
   - Health check: `/health`

2. **TGBOT сервис** (`bot.py`)
   - Обрабатывает команды Telegram бота
   - Получает заявки через API

3. **Лендинг** (Netlify/Vercel/etc)
   - Статический сайт
   - Отправляет запросы на API сервис

---

**Готово!** После этих шагов все будет работать правильно! 🚀

