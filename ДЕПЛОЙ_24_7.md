# Инструкция: Настройка работы 24/7 на Railway

## ✅ Что уже должно быть настроено в Railway

### 1. Сервис для Telegram бота (`bot.py`)
- **Procfile**: `worker: python bot.py`
- **Переменные окружения**:
  - `BOT_TOKEN` - токен вашего Telegram бота
  - `API_URL` - URL вашего API сервиса (например: `https://your-api-name.up.railway.app`)
  - `BOT_URL` - URL HTTP сервера бота (если используется отдельно)
  - `PORT` - Railway установит автоматически

### 2. Сервис для API (`bot_api.py`)
- **Procfile**: `web: python bot_api.py`
- **Переменные окружения**:
  - `BOT_TOKEN` - токен вашего Telegram бота (тот же что для бота)
  - `BOT_URL` - URL HTTP сервера бота (если бот запущен отдельно)
  - `API_URL` - URL этого API сервиса (для внутренних ссылок)
  - `BOT_USERNAME` - username вашего бота (опционально)
  - `PORT` - Railway установит автоматически

## 🔧 Что нужно проверить/изменить

### 1. URL API в лендинге

**Файл**: `/Users/hayksarkisyan/Desktop/New Landing/calculate.js`

Сейчас указан локальный URL. Нужно изменить на Railway URL:

```javascript
// БЫЛО (для локальной разработки):
const API_URL = 'http://localhost:3001/api/process';

// ДОЛЖНО БЫТЬ (для продакшена):
const API_URL = 'https://your-api-name.up.railway.app/api/process';
```

**Где взять URL?**
1. Зайдите в Railway dashboard
2. Откройте ваш API сервис (где запущен `bot_api.py`)
3. Перейдите в **Settings** → **Networking**
4. Скопируйте **Public Domain** (например: `https://optimistic-cat-production-6d8e.up.railway.app`)
5. Добавьте `/api/process` в конец

### 2. URL API в боте

**Файл**: `/Users/hayksarkisyan/Desktop/TelegramBot/bot.py`

Проверьте строку 168. Для Railway должен быть правильный URL:

```python
# Для Railway используйте переменную окружения API_URL
api_url = os.getenv('API_URL', 'https://your-api-name.up.railway.app')
```

**Важно**: Убедитесь, что в Railway для сервиса бота установлена переменная окружения:
- `API_URL` = URL вашего API сервиса (например: `https://optimistic-cat-production-6d8e.up.railway.app`)

### 3. Переменные окружения в Railway

#### Для сервиса БОТА (`bot.py`):
```
BOT_TOKEN=ваш_токен_бота
API_URL=https://your-api-name.up.railway.app
BOT_URL=https://your-bot-http-service.up.railway.app (если используется)
```

#### Для сервиса API (`bot_api.py`):
```
BOT_TOKEN=ваш_токен_бота
BOT_URL=https://your-bot-http-service.up.railway.app (если используется)
API_URL=https://your-api-name.up.railway.app
BOT_USERNAME=ваш_бот_username (опционально)
```

## 📋 Чеклист проверки

- [ ] API сервис запущен и доступен по публичному URL
- [ ] Бот сервис запущен (worker процесс)
- [ ] В `calculate.js` указан правильный Railway URL для API
- [ ] В Railway для бота установлена переменная `API_URL`
- [ ] В Railway для API установлена переменная `BOT_TOKEN`
- [ ] В Railway для бота установлена переменная `BOT_TOKEN`
- [ ] Лендинг развернут (статический хостинг или через Railway)

## 🌐 Развертывание лендинга

### Вариант 1: Railway (статический хостинг)

1. Создайте новый сервис в Railway
2. Выберите **Static Site**
3. Подключите репозиторий или загрузите файлы из `/Users/hayksarkisyan/Desktop/New Landing`
4. Railway автоматически определит статический сайт
5. Получите публичный URL

### Вариант 2: Netlify / Vercel

1. Зайдите на netlify.com или vercel.com
2. Подключите репозиторий или загрузите папку `/Users/hayksarkisyan/Desktop/New Landing`
3. Получите публичный URL
4. **Важно**: После деплоя обновите `calculate.js` с правильным API URL

### Вариант 3: GitHub Pages

1. Создайте репозиторий на GitHub
2. Загрузите файлы из `/Users/hayksarkisyan/Desktop/New Landing`
3. Включите GitHub Pages в настройках репозитория
4. Получите URL вида: `https://username.github.io/repo-name`

## 🔍 Проверка работы

1. **Проверьте API**:
   - Откройте: `https://your-api-name.up.railway.app/health`
   - Должно вернуть: `{"status":"ok"}`

2. **Проверьте бота**:
   - Напишите боту `/start` в Telegram
   - Бот должен ответить

3. **Проверьте лендинг**:
   - Откройте ваш лендинг
   - Попробуйте загрузить файл
   - Проверьте, что форма отправляется на правильный API URL

## ⚠️ Важные моменты

1. **CORS**: API должен разрешать запросы с вашего лендинга. В `bot_api.py` уже настроен CORS для всех источников.

2. **Файлы**: Railway использует временное хранилище. Файлы `pending_orders.json`, `user_mapping.json` будут сохраняться, но при перезапуске сервиса могут быть потеряны (если не используется постоянное хранилище).

3. **Логи**: Все логи можно смотреть в Railway dashboard в разделе **Deployments** → выберите деплой → **View Logs**.

4. **Перезапуск**: Railway автоматически перезапускает сервисы при падении (если настроено в `railway.json`).

## 🚀 После настройки

После того как все настроено:
1. Обновите `calculate.js` с правильным Railway URL
2. Закоммитьте изменения (если используете Git)
3. Railway автоматически перезапустит сервисы
4. Проверьте работу всех компонентов

---

**Готово!** Теперь ваш бот и сайт будут работать 24/7 без необходимости держать терминалы открытыми.

