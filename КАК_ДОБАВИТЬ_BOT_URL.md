# Как добавить BOT_URL в Railway

## 📋 Пошаговая инструкция

### Шаг 1: Найдите сервис API в Railway

1. Откройте [Railway Dashboard](https://railway.app)
2. Войдите в свой аккаунт
3. Найдите проект, где у вас запущен **API сервис** (тот, где запущен `bot_api.py`)
4. **Кликните на сервис API** (не на TGBOT!)

### Шаг 2: Откройте раздел Variables

1. В открывшемся сервисе API найдите вкладки вверху:
   - **Deployments**
   - **Metrics**
   - **Variables** ← **Кликните сюда!**
   - **Settings**
   - и т.д.

2. Кликните на вкладку **Variables** (Переменные окружения)

### Шаг 3: Добавьте новую переменную

1. Вы увидите список существующих переменных (например, `BOT_TOKEN`, `PORT` и т.д.)
2. Найдите кнопку **"+ New Variable"** или **"Add Variable"** (обычно справа вверху или внизу списка)
3. Кликните на эту кнопку

### Шаг 4: Введите данные

Появится форма с двумя полями:

**В поле "Name" (Имя переменной):**
```
BOT_URL
```

**В поле "Value" (Значение):**
Вставьте URL, который вы скопировали из сервиса TGBOT. Например:
```
https://tgbot-production-abc123.up.railway.app
```

**⚠️ Важно:**
- Вставляйте URL **БЕЗ** `/api/...` в конце
- Вставляйте URL **БЕЗ** пробелов в начале и конце
- URL должен начинаться с `https://`

### Шаг 5: Сохраните

1. Кликните кнопку **"Add"** или **"Save"**
2. Railway автоматически перезапустит сервис API
3. Дождитесь завершения деплоя (статус станет "Running")

## 📸 Визуальная схема

```
Railway Dashboard
    ↓
Ваш проект
    ↓
┌─────────────────┐
│  API Service    │ ← Кликните сюда
└─────────────────┘
    ↓
┌─────────────────┐
│   Variables     │ ← Откройте эту вкладку
└─────────────────┘
    ↓
┌─────────────────┐
│  + New Variable │ ← Нажмите эту кнопку
└─────────────────┘
    ↓
┌─────────────────┐
│ Name:  BOT_URL  │
│ Value: https:// │ ← Вставьте URL сюда
│        tgbot... │
└─────────────────┘
```

## ✅ Проверка

После добавления переменной:

1. Убедитесь, что переменная `BOT_URL` появилась в списке
2. Проверьте, что значение правильное (без `/api/...` в конце)
3. Дождитесь завершения деплоя
4. Проверьте логи API сервиса - не должно быть ошибок

## 🔍 Где найти URL из TGBOT?

Если вы еще не скопировали URL:

1. В Railway Dashboard найдите сервис **TGBOT** (где запущен `bot.py`)
2. Кликните на него
3. Перейдите в **Settings** → **Networking**
4. Найдите **Public Domain**
5. Скопируйте URL (например: `https://tgbot-production-abc123.up.railway.app`)

## ❓ Частые вопросы

**Q: Что если у меня нет кнопки "+ New Variable"?**  
A: Убедитесь, что вы находитесь в правильном сервисе (API, а не TGBOT). Также попробуйте обновить страницу.

**Q: Нужно ли перезапускать сервис вручную?**  
A: Нет, Railway автоматически перезапустит сервис после добавления переменной.

**Q: Что если я вставил URL неправильно?**  
A: Просто кликните на переменную `BOT_URL` в списке, измените значение и сохраните.

**Q: Нужно ли добавлять `/api/user_chat_id` в конец URL?**  
A: Нет! Вставляйте только базовый URL, например: `https://tgbot-production-abc123.up.railway.app`

## 🎉 Готово!

После добавления `BOT_URL` система автоматической отправки заявок начнет работать!


