# Быстрая проверка Railway сервисов

## 🔍 Что проверить в Railway Dashboard

### 1. Сервис для API (`bot_api.py`)

**Проверьте:**
- ✅ Сервис запущен (статус "Running")
- ✅ Есть публичный домен (Settings → Networking → Public Domain)
- ✅ Переменные окружения:
  - `BOT_TOKEN` - установлен
  - `PORT` - Railway установит автоматически
  - `BOT_URL` - если используется (URL HTTP сервера бота)
  - `API_URL` - URL этого сервиса (для внутренних ссылок)

**Проверка работы:**
- Откройте: `https://your-api-domain.up.railway.app/health`
- Должно вернуть: `{"status":"ok"}`

### 2. Сервис для Бота (`bot.py`)

**Проверьте:**
- ✅ Сервис запущен (статус "Running")
- ✅ Procfile содержит: `worker: python bot.py`
- ✅ Переменные окружения:
  - `BOT_TOKEN` - установлен (тот же что для API)
  - `API_URL` - URL вашего API сервиса (например: `https://optimistic-cat-production-6d8e.up.railway.app`)
  - `BOT_URL` - если используется отдельный HTTP сервер для бота

**Проверка работы:**
- Напишите боту `/start` в Telegram
- Бот должен ответить

### 3. Что НЕ нужно менять в Railway

✅ **Procfile** - уже настроен правильно
✅ **railway.json** - уже настроен правильно  
✅ **requirements.txt** - уже содержит все зависимости
✅ **Код** - уже готов для работы на Railway

### 4. Что НУЖНО изменить

#### В файле `calculate.js` (лендинг):

Измените URL API с локального на Railway:

```javascript
// СЕЙЧАС (локальный):
const API_URL = 'http://localhost:3001/api/process';

// НУЖНО (Railway):
const API_URL = 'https://optimistic-cat-production-6d8e.up.railway.app/api/process';
```

**Где взять URL?**
1. Railway Dashboard → ваш API сервис
2. Settings → Networking
3. Скопируйте Public Domain
4. Добавьте `/api/process`

## 📝 Краткий чеклист

- [ ] API сервис запущен и доступен по `/health`
- [ ] Бот сервис запущен (worker процесс)
- [ ] В Railway для бота установлена переменная `API_URL` = URL вашего API сервиса
- [ ] В `calculate.js` указан правильный Railway URL (вместо localhost)
- [ ] Лендинг развернут на хостинге (Netlify/Vercel/Railway/GitHub Pages)

## ✅ Готово!

После этих проверок все будет работать 24/7!

