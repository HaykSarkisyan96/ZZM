# Как найти правильный API URL

## ⚠️ Проблема

URL `optimistic-cat-production-6d8e.up.railway.app` ведет на старый сайт, а не на API.

## 🔍 Как найти правильный API сервис

### Способ 1: Проверка всех сервисов в Railway

1. Зайдите в Railway Dashboard: https://railway.app
2. Откройте ваш проект
3. Посмотрите **все сервисы** в проекте
4. Для каждого сервиса:
   - Нажмите на сервис
   - Перейдите в **Settings** → **Networking**
   - Посмотрите **Public Domain**
   - Проверьте URL: `https://domain.up.railway.app/health`
   
   **Если возвращает `{"status":"ok"}`** - это API сервис!

### Способ 2: По логам сервисов

1. Откройте каждый сервис в Railway
2. Перейдите в **Logs**
3. Ищите в логах:
   - `Serving Flask app 'bot_api'`
   - `Running on http://0.0.0.0:PORT`
   - `* Running on all addresses`
   
   **Если видите это** - это API сервис!

### Способ 3: По Procfile/Start Command

1. Откройте каждый сервис
2. Перейдите в **Settings** → **Build & Deploy**
3. Посмотрите **Start Command** или Procfile
4. Если там `python bot_api.py` - это API сервис!

## ✅ После того как нашли API сервис

1. Запишите **Public Domain** этого сервиса
2. API endpoint будет: `https://api-domain.up.railway.app/api/process`
3. Проверьте: `https://api-domain.up.railway.app/health` - должно вернуть `{"status":"ok"}`

## 🔧 Если API сервиса нет

Если у вас нет отдельного сервиса для API, нужно:

### Вариант 1: Создать новый сервис для API

1. В Railway Dashboard нажмите **"New"** → **"Empty Service"**
2. Подключите ваш GitHub репозиторий (где находится `bot_api.py`)
3. В настройках:
   - **Start Command**: `python bot_api.py`
   - Или создайте `Procfile` с: `web: python bot_api.py`
4. Установите переменные окружения:
   - `BOT_TOKEN` = ваш токен бота
   - `PORT` = Railway установит автоматически
5. Получите Public Domain в Settings → Networking

### Вариант 2: Использовать существующий сервис (если там запущен бот)

Если у вас один сервис где запущен `bot.py`, можно:
- Добавить API endpoint в тот же сервис
- Но лучше создать отдельный сервис для стабильности

## 📝 Что нужно сделать

1. **Найдите или создайте API сервис** в Railway
2. **Скопируйте Public Domain** API сервиса
3. **Обновите `calculate.js`** с правильным URL:
   ```javascript
   const API_URL = 'https://ваш-api-домен.up.railway.app/api/process';
   ```

## 🧪 Проверка

После того как нашли правильный URL:

1. Откройте: `https://ваш-api-домен.up.railway.app/health`
2. Должно вернуть: `{"status":"ok"}`
3. Если вернуло - это правильный API сервис!

---

**Важно**: API сервис должен быть **отдельным** от сервиса со старым сайтом!

