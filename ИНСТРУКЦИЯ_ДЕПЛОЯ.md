# Инструкция по деплою через терминал

## 🚀 Быстрый деплой

### Вариант 1: Использовать скрипт deploy.sh (рекомендуется)

1. Откройте терминал
2. Перейдите в папку проекта:
   ```bash
   cd "/Users/hayksarkisyan/Desktop/New Landing"
   ```
3. Сделайте скрипт исполняемым (только первый раз):
   ```bash
   chmod +x deploy.sh
   ```
4. Запустите скрипт:
   ```bash
   ./deploy.sh
   ```
5. Введите сообщение для коммита (или нажмите Enter для стандартного)
6. Готово! Изменения будут отправлены на GitHub

### Вариант 2: С указанием сообщения коммита

```bash
./deploy.sh "Ваше сообщение коммита"
```

Например:
```bash
./deploy.sh "Добавлен новый текст сообщения после оплаты"
```

### Вариант 3: Ручной деплой (без скрипта)

Выполните команды по очереди:

```bash
cd "/Users/hayksarkisyan/Desktop/New Landing"
git add .
git commit -m "Ваше сообщение коммита"
git push
```

## 📋 Что делает скрипт deploy.sh

1. ✅ Проверяет, что вы в правильной папке
2. ✅ Показывает текущий статус изменений
3. ✅ Добавляет все измененные файлы
4. ✅ Позволяет ввести сообщение для коммита
5. ✅ Создает коммит
6. ✅ Отправляет изменения на GitHub
7. ✅ Показывает ссылку на сайт для проверки

## ⚠️ Возможные проблемы

### Проблема: "Permission denied" при запуске скрипта

**Решение:**
```bash
chmod +x deploy.sh
```

### Проблема: "Git repository not initialized"

**Решение:**
```bash
git init
git branch -M main
```

### Проблема: "Remote 'origin' not configured"

**Решение:**
```bash
git remote add origin https://github.com/ВАШ-USERNAME/НАЗВАНИЕ-РЕПОЗИТОРИЯ.git
```

Замените:
- `ВАШ-USERNAME` - ваш username на GitHub
- `НАЗВАНИЕ-РЕПОЗИТОРИЯ` - название репозитория

Например:
```bash
git remote add origin https://github.com/hayksarkisyan96/ZZM.git
```

### Проблема: "Authentication failed" при push

**Решение:**

1. Используйте Personal Access Token вместо пароля
   - Зайдите на GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Создайте новый токен с правами `repo`
   - Используйте токен вместо пароля при push

2. Или настройте SSH ключ:
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   # Добавьте публичный ключ на GitHub
   ```

## ✅ После успешного деплоя

1. Подождите **1-2 минуты** - GitHub Pages обновляется автоматически
2. Проверьте сайт: https://hayksarkisyan96.github.io/ZZM
3. Обновите страницу (Ctrl+F5 или Cmd+Shift+R) чтобы увидеть изменения

## 🎯 Примеры использования

### Деплой с описательным сообщением:
```bash
./deploy.sh "Улучшено сообщение после оплаты: добавлено приветствие и инструкции"
```

### Деплой после исправления бага:
```bash
./deploy.sh "Исправлена проверка подписки перед оплатой"
```

### Деплой после добавления новой функции:
```bash
./deploy.sh "Добавлена секция совместимых программ"
```

## 📝 Советы

1. **Всегда проверяйте изменения перед деплоем:**
   ```bash
   git status
   git diff
   ```

2. **Используйте понятные сообщения коммитов:**
   - ❌ Плохо: "fix"
   - ✅ Хорошо: "Исправлена проверка подписки перед оплатой"

3. **Делайте коммиты часто:**
   - После каждого значимого изменения
   - Не копите много изменений в одном коммите

4. **Проверяйте сайт после деплоя:**
   - Откройте сайт через 1-2 минуты
   - Убедитесь, что изменения применились
   - Проверьте консоль браузера на ошибки (F12)

## 🎉 Готово!

Теперь вы можете легко деплоить изменения одной командой:
```bash
./deploy.sh
```



