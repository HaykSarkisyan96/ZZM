# 🚀 Быстрый деплой через терминал

## Самый простой способ

Откройте терминал и выполните:

```bash
cd "/Users/hayksarkisyan/Desktop/New Landing"
chmod +x deploy.sh
./deploy.sh
```

Готово! Изменения будут отправлены на GitHub.

---

## 📝 Варианты деплоя

### 1. Полный скрипт (с выбором сообщения коммита)
```bash
./deploy.sh
```
Скрипт предложит ввести сообщение для коммита.

### 2. С указанием сообщения коммита
```bash
./deploy.sh "Ваше сообщение"
```

### 3. Ручной деплой (без скрипта)
```bash
cd "/Users/hayksarkisyan/Desktop/New Landing"
git add .
git commit -m "Ваше сообщение"
git push
```

---

## 🔧 Первая настройка (только один раз)

### 1. Сделать скрипты исполняемыми:
```bash
chmod +x deploy.sh
chmod +x deploy-simple.sh
```

### 2. Настроить remote (если еще не настроен):
```bash
git remote add origin https://github.com/hayksarkisyan96/ZZM.git
```

---

## ⚡ Быстрая справка

| Команда | Описание |
|---------|----------|
| `./deploy.sh` | Полный деплой с выбором сообщения |
| `./deploy.sh "текст"` | Деплой с указанным сообщением |
| `git status` | Показать изменения |
| `git log --oneline -5` | Последние 5 коммитов |

---

## 📍 После деплоя

1. Подождите **1-2 минуты**
2. Проверьте сайт: https://hayksarkisyan96.github.io/ZZM
3. Обновите страницу (Cmd+Shift+R)

---

## ❓ Если что-то не работает

Подробные инструкции смотрите в файле:
- `ИНСТРУКЦИЯ_ДЕПЛОЯ.md` - подробная инструкция
- `ДЕПЛОЙ_ИЗМЕНЕНИЙ.md` - примеры использования



