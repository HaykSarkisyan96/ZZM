# Исправление ошибки "Not Found" в API

## 🔍 Диагностика проблемы

Ошибка "Not Found" означает, что сервер работает, но не может найти запрашиваемый путь. Это может быть из-за:

1. ❌ Неправильный Start Command в Railway
2. ❌ Сервис запускает не `bot_api.py`
3. ❌ Проблема с маршрутизацией

## ✅ Шаг 1: Проверка Start Command

1. Откройте Railway Dashboard
2. Откройте сервис `new-landing-production`
3. Перейдите в **Settings** → **Build & Deploy**
4. Проверьте **Start Command**

**Должно быть:**
```
python bot_api.py
```

**Или если используется Procfile:**
```
web: python bot_api.py
```

**Если неправильно:**
- Измените на `python bot_api.py`
- Сохраните
- Railway автоматически перезапустит сервис

## ✅ Шаг 2: Проверка логов

1. В Railway откройте сервис `new-landing-production`
2. Перейдите в **Logs** (вкладка вверху)
3. Посмотрите последние логи

**Что искать:**
- ✅ `Serving Flask app 'bot_api'` - правильно
- ✅ `Running on http://0.0.0.0:PORT` - правильно
- ❌ `Serving Flask app 'bot'` - неправильно, запускается бот вместо API
- ❌ Ошибки импорта или запуска

## ✅ Шаг 3: Проверка health endpoint

Попробуйте открыть в браузере:
```
https://new-landing-production.up.railway.app/health
```

**Если вернуло `{"status":"ok"}`:**
- ✅ API работает правильно
- ✅ Проблема может быть в пути `/api/process`

**Если вернуло "Not Found":**
- ❌ API не запущен правильно
- ❌ Нужно проверить Start Command

## ✅ Шаг 4: Проверка структуры файлов

Убедитесь, что в репозитории есть:
- ✅ `bot_api.py` в корне репозитория
- ✅ `requirements.txt` с зависимостями
- ✅ `Procfile` (опционально)

## 🔧 Шаг 5: Исправление (если Start Command неправильный)

### Вариант A: Через Railway Dashboard

1. Откройте сервис в Railway
2. **Settings** → **Build & Deploy**
3. В разделе **Start Command** укажите:
   ```
   python bot_api.py
   ```
4. Сохраните
5. Railway перезапустит сервис

### Вариант B: Через Procfile

1. В вашем репозитории создайте/обновите файл `Procfile`
2. Добавьте:
   ```
   web: python bot_api.py
   ```
3. Закоммитьте и запушьте в GitHub
4. Railway автоматически пересоберет сервис

### Вариант C: Через railway.json

1. В Railway откройте сервис
2. **Settings** → **Build & Deploy**
3. Найдите **"Open File"** рядом с Custom Start Command
4. Откроется `railway.json`
5. Измените:
   ```json
   {
     "deploy": {
       "startCommand": "python bot_api.py"
     }
   }
   ```
6. Сохраните

## 🧪 Шаг 6: Проверка после исправления

1. Дождитесь завершения деплоя (статус "Running")
2. Проверьте логи - должны увидеть:
   ```
   Serving Flask app 'bot_api'
   Running on http://0.0.0.0:PORT
   ```
3. Откройте: `https://new-landing-production.up.railway.app/health`
4. Должно вернуть: `{"status":"ok"}`
5. Если работает - API настроен правильно! ✅

## 📋 Чеклист исправления

- [ ] Start Command установлен: `python bot_api.py`
- [ ] В логах видно: `Serving Flask app 'bot_api'`
- [ ] `/health` endpoint возвращает `{"status":"ok"}`
- [ ] Сервис имеет статус "Running"
- [ ] Переменная `BOT_TOKEN` установлена

## ⚠️ Частые ошибки

1. **Запускается `bot.py` вместо `bot_api.py`**
   - Решение: Измените Start Command на `python bot_api.py`

2. **Нет переменной `BOT_TOKEN`**
   - Решение: Добавьте в Variables → `BOT_TOKEN=ваш_токен`

3. **Ошибки импорта в логах**
   - Решение: Проверьте `requirements.txt`, все зависимости должны быть указаны

4. **Сервис не запускается**
   - Решение: Проверьте логи на наличие ошибок

---

**После исправления** API должен работать правильно! 🚀

